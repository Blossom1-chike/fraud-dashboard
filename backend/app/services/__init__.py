# This folder holds the business logic — the actual steps behind an action,
# like "log this user in" or "save this uploaded dataset". API routes call
# into here, and this layer calls the repositories to touch the database.
