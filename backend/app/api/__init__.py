# This folder is the "front door" of the backend: the actual URLs the
# frontend calls, like /auth/login or /datasets/upload. Each file here
# groups together routes for one feature. These files mostly just receive
# the request and hand off the real work to a service.
