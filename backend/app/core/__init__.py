# This folder holds the app's foundational setup: settings/config, the
# database connection, and security helpers like password hashing and
# JWT tokens. Almost every other folder depends on something in here.
